{
  "name": "chartforge-rsi",
  "private": true,
  "version": "0.4.1",
  "type": "module",
  "scripts": {
    "build:pwa": "node scripts/build-pwa-canonical.mjs",
    "test:pwa": "node --test tests/*.test.mjs",
    "validate:pwa": "node scripts/validate-pwa.mjs",
    "test": "npm run build:pwa && npm run validate:pwa && npm run test:pwa"
  }
}
