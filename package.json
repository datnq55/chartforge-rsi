{
  "name": "chartforge-rsi",
  "private": true,
  "version": "0.4.2",
  "type": "module",
  "scripts": {
    "build:pwa": "node scripts/build-pwa-canonical.mjs",
    "test:pwa": "node --test tests/pwa*.test.mjs",
    "test:rules": "npx --yes firebase-tools@15.29.0 emulators:exec --only firestore --project demo-chartforge-rsi \"node --test tests/firestore-rules.test.mjs\"",
    "smoke:pwa": "node scripts/smoke-pwa.mjs",
    "validate:pwa": "node scripts/validate-pwa.mjs",
    "test": "npm run build:pwa && npm run validate:pwa && npm run test:pwa && npm run test:rules"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "playwright-core": "^1.55.0"
  }
}
